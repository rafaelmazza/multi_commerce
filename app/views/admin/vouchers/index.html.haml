- content_for :title, 'Vouchers'
= render 'admin/shared/navbar'
  
%table.table
  %thead
    %tr
      %th= check_box_tag ''
      %th Código
      %th Total
      %th Status
  %tbody
  - @vouchers.each do |lead|
    %tr
      %td= check_box_tag ''
      %td= lead.code
      %td= lead.total
      %td
        %span.label.label-warning= lead.status

= paginate @vouchers